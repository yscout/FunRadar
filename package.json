{
  "name": "funradar",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd backend && npm install --production=false && npm run build:original",
    "start": "cd backend && bundle exec rails server -p $PORT"
  }
}